# Backlog

- Commit history chaining between commit files (incremental log traversal).
- Garbage collection of unreachable blobs/commit files.
- Typed schemas beyond `string` (numeric, timestamp, boolean).
- Conflict/merge strategy for concurrent writes on the same dataset.
- Optional compaction of long commit histories into snapshots.
- Repo introspection/debug commands (inspect index, collection, commit metadata).
